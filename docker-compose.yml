version: '3.8'

services:
  youtube-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
